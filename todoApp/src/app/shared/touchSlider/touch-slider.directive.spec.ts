import { TouchSliderDirective } from './touch-slider.directive';

describe('TouchSliderDirective', () => {
  it('should create an instance', () => {
    const directive = new TouchSliderDirective();
    expect(directive).toBeTruthy();
  });
});
