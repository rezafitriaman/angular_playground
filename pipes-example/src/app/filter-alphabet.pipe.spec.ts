import { FilterAlphabetPipe } from './filter-alphabet.pipe';

describe('FilterAlphabetPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAlphabetPipe();
    expect(pipe).toBeTruthy();
  });
});
