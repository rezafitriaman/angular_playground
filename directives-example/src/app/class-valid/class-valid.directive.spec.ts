import { ClassValidDirective } from './class-valid.directive';

describe('ClassValidDirective', () => {
  it('should create an instance', () => {
    const directive = new ClassValidDirective();
    expect(directive).toBeTruthy();
  });
});
