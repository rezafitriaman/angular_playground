import { BeterHeighlighDirective } from './beter-heighligh.directive';

describe('BeterHeighlighDirective', () => {
  it('should create an instance', () => {
    const directive = new BeterHeighlighDirective();
    expect(directive).toBeTruthy();
  });
});
